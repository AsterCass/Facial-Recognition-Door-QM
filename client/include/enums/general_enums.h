#ifndef GENERAL_ENUMS_H
#define GENERAL_ENUMS_H

enum LogLevel {
    TRACE,
    DEBUG,
    INFO,
    WARN,
    ERROR,
    CRITICAL
};

enum LogPrintType {
    CONSOLE,
    OFILE,
};

enum MainPage {
    MAIN_PAGE_INIT,
    MAIN_PAGE_HOME,
};

#endif //GENERAL_ENUMS_H
